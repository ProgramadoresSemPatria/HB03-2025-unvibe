{
  "name": "unvibe-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "bot",
    "web-app",
    "packages/db"
  ],
  "scripts": {
    "run:bot": "npm run build --workspace=unvibe && npm start --workspace=unvibe",
    "build:bot": "npm run build --workspace=unvibe",
    "dev:web": "npm run dev --workspace=web-app",
    "build:web": "npm run build --workspace=web-app",
    "start:web": "npm start --workspace=web-app"
  }
}

